function warped = diskwarp(img)
    height = size(img, 1);
    width = size(img, 2);
    
    for v=1:height
        for u=1:width
            x = a0+a1*u+a2*v+a3*u*u+
        end
    end
    
    
    